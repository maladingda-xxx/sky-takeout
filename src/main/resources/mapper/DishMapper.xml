<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.takeout.mapper.DishMapper">

    <resultMap id="dishFlavorResultMap" type="DishFlavor">
        <id column="id" property="id"/>
        <result column="dish_id" property="dishId"/>
        <result column="name" property="name"/>
        <result column="value_json" property="value"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_user" property="createUser"/>
        <result column="update_user" property="updateUser"/>
    </resultMap>

    <sql id="queryWhere">
        <where>
            <if test="name != null and name != ''">
                AND d.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="categoryId != null">
                AND d.category_id = #{categoryId}
            </if>
            <if test="status != null">
                AND d.status = #{status}
            </if>
        </where>
    </sql>

    <select id="countByQuery" resultType="long">
        SELECT COUNT(*)
        FROM dish d
        <include refid="queryWhere"/>
    </select>

    <select id="selectPage" resultType="com.sky.takeout.vo.DishPageVO">
        SELECT d.id,
               d.name,
               d.category_id,
               c.name AS category_name,
               d.price,
               d.image,
               d.status,
               d.update_time
        FROM dish d
        LEFT JOIN category c ON c.id = d.category_id
        <include refid="queryWhere"/>
        ORDER BY d.update_time DESC, d.id DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dish (
            name,
            category_id,
            price,
            image,
            description,
            status,
            create_user,
            update_user
        ) VALUES (
            #{name},
            #{categoryId},
            #{price},
            #{image},
            #{description},
            #{status},
            #{createUser},
            #{updateUser}
        )
    </insert>

    <select id="selectById" resultType="Dish">
        SELECT id,
               name,
               category_id,
               price,
               image,
               description,
               status,
               create_time,
               update_time,
               create_user,
               update_user
        FROM dish
        WHERE id = #{id}
    </select>

    <update id="update">
        UPDATE dish
        SET name = #{name},
            category_id = #{categoryId},
            price = #{price},
            image = #{image},
            description = #{description},
            update_time = CURRENT_TIMESTAMP,
            update_user = #{updateUser}
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE dish
        SET status = #{status},
            update_time = CURRENT_TIMESTAMP,
            update_user = #{updateUser}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM dish
        WHERE id = #{id}
    </delete>

    <insert id="insertFlavors">
        INSERT INTO dish_flavor (
            dish_id,
            name,
            value_json,
            create_user,
            update_user
        ) VALUES
        <foreach collection="flavors" item="flavor" separator=",">
            (
                #{flavor.dishId},
                #{flavor.name},
                #{flavor.value},
                #{flavor.createUser},
                #{flavor.updateUser}
            )
        </foreach>
    </insert>

    <delete id="deleteFlavorsByDishId">
        DELETE FROM dish_flavor
        WHERE dish_id = #{dishId}
    </delete>

    <select id="selectFlavorsByDishId" resultMap="dishFlavorResultMap">
        SELECT id,
               dish_id,
               name,
               value_json,
               create_time,
               update_time,
               create_user,
               update_user
        FROM dish_flavor
        WHERE dish_id = #{dishId}
        ORDER BY id ASC
    </select>

    <select id="selectFlavorsByDishIds" resultMap="dishFlavorResultMap">
        SELECT id,
               dish_id,
               name,
               value_json,
               create_time,
               update_time,
               create_user,
               update_user
        FROM dish_flavor
        WHERE dish_id IN
        <foreach collection="dishIds" item="dishId" open="(" separator="," close=")">
            #{dishId}
        </foreach>
        ORDER BY dish_id ASC, id ASC
    </select>

    <select id="selectEnabledByCategoryId" resultType="Dish">
        SELECT id,
               name,
               category_id,
               price,
               image,
               description,
               status,
               update_time
        FROM dish
        WHERE category_id = #{categoryId}
          AND status = 1
        ORDER BY update_time DESC, id DESC
    </select>

    <select id="countSetmealsByDishId" resultType="long">
        SELECT COUNT(*)
        FROM setmeal_dish
        WHERE dish_id = #{dishId}
    </select>

</mapper>

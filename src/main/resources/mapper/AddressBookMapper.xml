<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.takeout.mapper.AddressBookMapper">

    <sql id="addressColumns">
        id,
        user_id,
        consignee,
        sex,
        phone,
        province_name,
        city_name,
        district_name,
        detail,
        label,
        is_default,
        create_time,
        update_time
    </sql>

    <select id="selectByUserId" resultType="AddressBook">
        SELECT <include refid="addressColumns"/>
        FROM address_book
        WHERE user_id = #{userId}
        ORDER BY is_default DESC, update_time DESC, id DESC
    </select>

    <select id="selectByIdAndUserId" resultType="AddressBook">
        SELECT <include refid="addressColumns"/>
        FROM address_book
        WHERE id = #{id}
          AND user_id = #{userId}
    </select>

    <select id="selectDefaultByUserId" resultType="AddressBook">
        SELECT <include refid="addressColumns"/>
        FROM address_book
        WHERE user_id = #{userId}
          AND is_default = 1
        LIMIT 1
    </select>

    <select id="selectFirstByUserId" resultType="AddressBook">
        SELECT <include refid="addressColumns"/>
        FROM address_book
        WHERE user_id = #{userId}
        ORDER BY update_time DESC, id DESC
        LIMIT 1
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(*)
        FROM address_book
        WHERE user_id = #{userId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO address_book (
            user_id,
            consignee,
            sex,
            phone,
            province_name,
            city_name,
            district_name,
            detail,
            label,
            is_default
        ) VALUES (
            #{userId},
            #{consignee},
            #{sex},
            #{phone},
            #{provinceName},
            #{cityName},
            #{districtName},
            #{detail},
            #{label},
            #{isDefault}
        )
    </insert>

    <update id="update">
        UPDATE address_book
        SET consignee = #{consignee},
            sex = #{sex},
            phone = #{phone},
            province_name = #{provinceName},
            city_name = #{cityName},
            district_name = #{districtName},
            detail = #{detail},
            label = #{label},
            is_default = #{isDefault},
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND user_id = #{userId}
    </update>

    <update id="clearDefaultByUserId">
        UPDATE address_book
        SET is_default = 0,
            update_time = CURRENT_TIMESTAMP
        WHERE user_id = #{userId}
          AND is_default = 1
    </update>

    <update id="setDefault">
        UPDATE address_book
        SET is_default = 1,
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND user_id = #{userId}
    </update>

    <delete id="deleteByIdAndUserId">
        DELETE FROM address_book
        WHERE id = #{id}
          AND user_id = #{userId}
    </delete>

</mapper>

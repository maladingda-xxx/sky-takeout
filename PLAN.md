# Sky Takeout 开发计划

本计划以可运行、可测试、可复盘为完成标准。每一轮只完成一个边界清晰的目标，并在完成后更新 `LEARNING.md`。

## 状态说明

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成

## 第一轮迭代：员工分页 API 工程化

目标：在不引入新业务模块的前提下，把已经实现的员工分页查询补成后续模块可以复用的 API 基线。

验收标准：

- 接口统一返回 `code`、`message`、`data` 三个字段。
- `page`、`pageSize`、`status`、`name` 具有明确的参数约束。
- 参数错误返回 HTTP 400，而不是默认的 500。
- 未预期异常返回 HTTP 500，同时保留服务端日志。
- Controller、Service、Mapper 三层都有对应测试覆盖。
- `LEARNING.md` 记录本轮设计、代码、调用链和验证方法。

任务：

- [x] 定义统一响应对象 `Result<T>`。
- [x] 为员工分页查询增加参数校验。
- [x] 增加统一异常处理器和业务异常类型。
- [x] 增加 Controller 层参数与响应测试。
- [x] 增加基于 H2 的 MyBatis 集成测试。
- [x] 增加独立测试配置，避免测试依赖本机 MySQL。
- [x] 运行全部自动化测试。
- [x] 把本轮执行过程写成 `LEARNING.md` 教程。

## 第二轮迭代：员工管理完整 CRUD

目标：围绕 `employee` 表完成管理端员工维护闭环。

- [x] 新增员工，校验用户名和身份证号唯一性。
- [x] 密码使用 BCrypt 加密后保存。
- [x] 根据 ID 查询员工详情。
- [x] 修改员工信息。
- [x] 启用或禁用员工账号。
- [x] 删除员工。
- [x] 防止删除或禁用当前登录管理员；该规则依赖 Phase 2 的登录身份。
- [x] 为写操作增加事务边界测试。
- [x] 使用 H2 完成 Controller、Service、Mapper 集成测试。
- [x] 使用本机 MySQL 执行 `sql/schema.sql` 并完成真实数据库验收。

## 后续迭代

分类、菜品、套餐、登录认证、用户端业务、管理端订单闭环、KDS 接入和 Redis 缓存等迭代按模块推进，每一轮的目标、代码链路、踩坑和验证结果记录在 `LEARNING.md`，此处不再重复罗列。

## Phase 1：基础业务

- [x] 员工管理。
- [x] 分类管理。
- [x] 菜品管理。
- [x] 套餐管理。
- [x] 图片上传。

## Phase 2：登录认证

- [x] 管理端员工登录。
- [~] 用户端微信登录：mock 和代码调用链已完成，等待微信 AppID/AppSecret 联调。
- [x] JWT 生成与校验。
- [x] 登录拦截器。
- [x] 登录状态与权限边界。
- [x] 防止删除或禁用当前登录管理员。

## Phase 3：用户端业务

- [x] 菜品浏览。
- [x] 套餐浏览。
- [x] 购物车。
- [x] 收货地址。
- [x] 下单。
- [x] 支付状态模拟。
- [x] 订单查询与状态流转。

## 增量迭代：管理端订单闭环

目标：让管理员能够处理完整订单状态，并把订单状态从“用户可下单”推进到“门店可履约”。

- [x] 管理端按订单号、手机号、状态和时间范围分页查询订单。
- [x] 管理端查询订单详情和订单明细。
- [x] 管理端接单，订单从待接单进入已接单。
- [x] 管理端拒单，并模拟已支付订单退款。
- [x] 管理端取消订单，并模拟已支付订单退款。
- [x] 管理端派送和完成订单。
- [x] 管理端查询待接单、已接单、派送中数量。
- [x] 修复支付与取消并发时可能覆盖支付状态的问题。
- [x] 增加 Controller、Service、Mapper 和 HTTP 集成测试。
- [x] 使用真实 MySQL 完成管理端订单验收。
- [x] 把本步执行过程写成 `LEARNING.md` 教程。

## 增量迭代：KDS 接入真实订单

目标：让厨房看板读取 Spring Boot 中的真实订单，并把拖拽和按钮操作写回订单状态。

- [x] 为 `little-fables` 增加 `/api` 后端代理，避免浏览器 CORS。
- [x] KDS 使用管理端账号登录并保存会话 Token。
- [x] KDS 轮询待接单、已接单和派送中订单。
- [x] 管理端订单分页支持 `withDetails=true` 批量返回订单明细。
- [x] 真实订单映射到 KDS 票据，显示订单号、商品、数量、金额、备注和电话。
- [x] KDS 按钮支持接单、派送和完成。
- [x] KDS 拖拽支持将订单推进到下一个状态。
- [x] 增加 Session 过期处理和手动刷新。
- [x] 完成桌面端和移动端浏览器验证。
- [x] 使用真实 MySQL 完成 KDS 端到端验收并清理数据。
- [x] 把本步执行过程写成 `LEARNING.md` 教程。

## Phase 4：缓存与异步

- [x] 安装并启动本机 Redis。
- [x] 配置 Spring Data Redis 连接、JSON 序列化和统一缓存 TTL。
- [x] 根据真实访问模式引入 Redis：用户端分类、菜品、套餐、套餐详情读取走缓存。
- [x] 设计菜品和套餐缓存。
- [x] 处理缓存一致性：管理端写操作按影响范围清理缓存。
- [x] 处理缓存穿透、击穿和雪崩：负缓存、加锁回源、TTL 抖动。
- [x] 评估消息队列是否确实需要：结论为暂不引入，触发条件见 `LEARNING.md` Step 19。

## Phase 5：工程化与部署

- [ ] 日志规范。
- [ ] AOP 记录操作日志。
- [ ] 接口文档。
- [ ] Docker 镜像。
- [ ] Nginx 反向代理。
- [ ] Linux 部署。
- [ ] 基础监控与故障排查。

## 持续执行规则

- 每次新增技术前，先说明它解决的具体问题。
- 每个阶段完成后运行测试，并记录验证结果。
- `LEARNING.md` 只记录已经执行并验证过的内容，不提前写成完成状态。
- 每个阶段形成一次独立提交，提交信息使用 `feat:`、`fix:`、`refactor:` 前缀。
